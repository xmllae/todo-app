# HTML 网站项目开发规范文档

> 本文档用于规范 HTML 网站项目的开发流程、代码风格与协作方式，AI 助手（如 Codex、Claude、Cursor 等）和人类开发者均需严格遵循。

---

## 一、项目目录结构

```
project-root/
├── index.html              # 主入口
├── pages/                  # 其他页面
│   ├── about.html
│   └── contact.html
├── assets/
│   ├── css/
│   │   ├── reset.css       # 样式重置
│   │   ├── variables.css   # CSS 变量（颜色、字号、间距等）
│   │   ├── common.css      # 全局通用样式
│   │   └── pages/          # 各页面专属样式
│   │       └── about.css
│   ├── js/
│   │   ├── utils.js        # 通用工具函数
│   │   ├── components/     # 可复用 UI 组件脚本
│   │   └── pages/          # 各页面专属脚本
│   ├── images/             # 图片资源（按模块分子目录）
│   ├── fonts/              # 字体文件
│   └── icons/              # 图标（SVG 优先）
├── components/             # 可复用 HTML 片段（如 header、footer）
├── docs/                   # 项目文档
└── README.md
```

**目录使用规则：**
- 新增文件前，先确认是否已有合适的目录，不要随意新建顶层目录。
- 资源文件按"通用 / 页面专属"分类放置，避免所有文件堆在根目录。
- 图片按业务模块分子目录（如 `images/banner/`、`images/products/`），不要全部平铺。

---

## 二、HTML 编码规范

### 2.1 文档结构
- 必须使用 HTML5 标准：`<!DOCTYPE html>`。
- 必须声明语言：`<html lang="zh-CN">`。
- 必须包含 viewport meta：`<meta name="viewport" content="width=device-width, initial-scale=1.0">`。
- `<title>` 必填，内容具有描述性，不得为空或写"未命名"。

### 2.2 语义化标签
- 优先使用语义标签：`<header>`、`<nav>`、`<main>`、`<section>`、`<article>`、`<aside>`、`<footer>`。
- 禁止滥用 `<div>` 包裹一切；只有在没有合适语义标签时才使用 `<div>`。
- 标题层级（`<h1>` ~ `<h6>`）必须按顺序使用，不得跳级，不得为了样式而选择标题级别。

### 2.3 属性与格式
- 所有属性值使用**双引号**。
- 属性名小写，自定义属性使用 `data-` 前缀。
- 自闭合标签不写斜杠（HTML5 风格）：`<img>` 而非 `<img />`。
- 缩进统一使用 **2 个空格**，不使用 Tab。
- 嵌套层级不超过 5 层，超过则需考虑拆分组件。

### 2.4 可访问性（A11y）
- 所有 `<img>` 必须有 `alt` 属性；装饰性图片用 `alt=""`。
- 表单控件必须配合 `<label>` 使用，或使用 `aria-label`。
- 交互元素（按钮、链接）必须有清晰可识别的文本或 `aria-label`。
- 颜色对比度需满足 WCAG AA 标准（正文 4.5:1，大字 3:1）。

### 2.5 禁止事项
- 禁止使用内联样式 `style="..."`，特殊情况除外（如动态计算值）。
- 禁止使用已废弃标签：`<center>`、`<font>`、`<marquee>` 等。
- 禁止使用 `<table>` 做布局，仅用于展示表格数据。
- 禁止留下注释掉的废弃代码。

---

## 三、CSS 编码规范

### 3.1 组织方式
- 使用 CSS 变量统一管理颜色、字号、间距、圆角、阴影，定义在 `variables.css`：
  ```css
  :root {
    --color-primary: #3b82f6;
    --color-text: #1f2937;
    --spacing-sm: 8px;
    --spacing-md: 16px;
    --radius-md: 8px;
    --font-size-base: 16px;
  }
  ```
- **禁止硬编码魔法值**：颜色、间距、字号必须使用变量。
- 通用样式放 `common.css`，页面专属样式放 `pages/` 下对应文件，互不污染。

### 3.2 命名规范（采用 BEM）
- 格式：`block__element--modifier`
- 示例：
  ```css
  .card { }
  .card__title { }
  .card__title--highlighted { }
  ```
- 类名小写，单词间用连字符（`-`），不使用驼峰或下划线分隔单词。
- 类名需有语义，禁止使用 `.box1`、`.div2`、`.red-text` 这类命名。

### 3.3 选择器规则
- 选择器嵌套不超过 3 层。
- 禁止使用 ID 选择器写样式（ID 仅用于 JS 选择和锚点）。
- 避免使用 `!important`；如必须使用，需注释说明原因。
- 避免标签选择器全局生效（如 `div { ... }`），应限定作用域。

### 3.4 响应式设计
- **移动优先**：默认样式为移动端，使用 `min-width` 媒体查询逐步增强。
- 断点统一定义，建议：
  - 移动端：默认
  - 平板：`min-width: 768px`
  - 桌面：`min-width: 1024px`
  - 大屏：`min-width: 1280px`
- 使用相对单位：`rem`、`em`、`%`、`vw/vh`，避免大量使用 `px`（边框、阴影等细节除外）。
- 图片必须设置 `max-width: 100%; height: auto;` 防止溢出。

### 3.5 属性顺序建议
```
1. 定位：position, top, right, bottom, left, z-index
2. 盒模型：display, flex, grid, width, height, margin, padding
3. 文字：font, line-height, color, text-align
4. 视觉：background, border, border-radius, box-shadow
5. 其他：opacity, transition, transform
```

---

## 四、JavaScript 编码规范

### 4.1 基础规则
- 使用 ES6+ 语法（`const`/`let`、箭头函数、模板字符串、解构等）。
- 禁止使用 `var`。
- 优先使用 `const`，确需重新赋值时才用 `let`。
- 字符串使用单引号 `'`，模板字符串使用反引号 `` ` ``。
- 缩进 2 个空格，语句末尾必须加分号。

### 4.2 命名规范
- 变量、函数：小驼峰 `camelCase`。
- 常量：全大写下划线 `MAX_COUNT`。
- 类、构造函数：大驼峰 `PascalCase`。
- 私有变量/函数：下划线前缀 `_privateVar`。
- 布尔值以 `is`、`has`、`can`、`should` 开头。
- 函数名为动词或动宾短语：`getUserInfo()`、`renderList()`。

### 4.3 函数与模块
- **单一职责**：一个函数只做一件事，超过 50 行需考虑拆分。
- 函数嵌套不超过 3 层。
- 函数参数不超过 3 个，超过则用对象传参。
- 使用 ES Modules（`import`/`export`），避免污染全局。
- 通用工具函数放 `utils.js`，禁止在业务代码中重复实现。

### 4.4 DOM 操作
- 优先使用 `querySelector` / `querySelectorAll`，避免使用过时的 `getElementById` 等（除非有性能需求）。
- 频繁操作的 DOM 元素需缓存引用，避免重复查询。
- 批量修改 DOM 时使用 `DocumentFragment` 或一次性更新 `innerHTML`。
- 事件监听优先使用事件委托，减少监听器数量。
- 必须在适当时机移除事件监听器，防止内存泄漏。

### 4.5 禁止事项
- 禁止使用 `eval()`、`with`、`document.write()`。
- 禁止滥用全局变量；必须挂载时使用统一命名空间。
- 禁止留下 `console.log`、`debugger`、`alert` 等调试代码。
- 禁止留下未使用的变量、函数、import。
- 禁止注释掉的死代码长期保留（超过 1 个版本应删除）。

---

## 五、资源管理

### 5.1 图片
- 优先使用现代格式：`WebP` > `AVIF`，并提供降级。
- 图标优先使用 SVG，其次字体图标，最后位图。
- 大图必须压缩，单张不超过 200KB（特殊场景除外）。
- 必须设置 `width` 和 `height` 属性防止布局抖动（CLS）。
- 首屏外图片使用 `loading="lazy"`。

### 5.2 字体
- 优先使用系统字体栈，减少加载体积。
- 使用 Web 字体时，必须设置 `font-display: swap;`。
- 子集化字体文件，仅保留所需字符。

### 5.3 第三方资源
- 引入第三方库前必须评估必要性，能用原生实现的不引入。
- 使用 CDN 时优先选择稳定可靠的源。
- 必须固定版本号，禁止使用 `latest`。

---

## 六、性能与优化

- HTML 文件大小尽量控制在合理范围。
- CSS 放 `<head>`，JS 放 `<body>` 末尾或使用 `defer`/`async`。
- 关键 CSS 内联，非关键资源延迟加载。
- 减少 HTTP 请求：合并小文件、雪碧图、SVG sprite。
- 使用浏览器缓存（合理设置 `Cache-Control`）。
- 避免阻塞渲染的资源；首屏渲染时间 ≤ 2 秒为佳。
- Lighthouse 评分目标：性能 ≥ 90，可访问性 ≥ 95。

---

## 七、浏览器兼容性

- 默认支持范围：最新两个版本的 Chrome、Edge、Firefox、Safari。
- 移动端：iOS Safari 14+、Android Chrome 90+。
- 使用新特性前查阅 [Can I Use](https://caniuse.com)，必要时提供降级方案。
- 不支持 IE，无需为其编写兼容代码。

---

## 八、注释规范

- 文件顶部注释说明文件用途、作者、最后修改日期（可选）。
- 复杂逻辑、特殊处理、Hack 必须有注释说明原因（**写"为什么"，不写"是什么"**）。
- 函数注释使用 JSDoc：
  ```js
  /**
   * 格式化金额显示
   * @param {number} amount - 原始金额
   * @param {string} [currency='¥'] - 货币符号
   * @returns {string} 格式化后的字符串
   */
  function formatMoney(amount, currency = '¥') { }
  ```
- TODO / FIXME 注释需注明负责人和预期处理时间。

---

## 九、Git 提交规范

提交信息格式：`<type>: <subject>`

**type 类型：**
- `feat`: 新功能
- `fix`: 修复 bug
- `style`: 样式调整（不影响逻辑）
- `refactor`: 重构（非新增功能也非修 bug）
- `perf`: 性能优化
- `docs`: 文档变更
- `chore`: 构建、配置等杂项

**示例：**
```
feat: 新增首页轮播图组件
fix: 修复移动端导航栏溢出问题
style: 调整产品卡片间距
```

---

## 十、AI 助手协作准则

当使用 Codex / Claude / Cursor 等 AI 助手时，**必须遵守以下规则**：

### 10.1 编码前
1. **先复述理解**：动手前简述对需求和现有代码的理解，等确认后再写。
2. **先给方案**：说明涉及哪些文件、做哪些改动、为什么这样改。
3. **检索复用**：新增功能前先搜索项目中是否已有同类工具/组件，优先复用。

### 10.2 编码中
1. **保持修改局部性**：不要顺手改无关代码；改 A 时不要"顺便"重构 B。
2. **遵循根因修复**：修 bug 时找根本原因，不要简单加 `if` 兜底。
3. **不过度设计**：当前不需要的抽象、参数、配置项一律不加。
4. **不重复造轮子**：项目已有的工具函数、组件、样式变量优先使用。
5. **小步前进**：每次只完成一个明确小目标，完成后停下等确认。

### 10.3 编码后自检
完成修改后必须回答：
1. 是否产生了重复代码？
2. 是否破坏了原有分层与模块边界？
3. 是否引入了未使用的代码、import、参数？
4. 命名是否清晰一致？
5. 是否硬编码了魔法值？
6. 是否留下了 `console.log`、注释掉的代码、`TODO` 占位？
7. 移动端、桌面端是否都正常显示？

### 10.4 禁止行为
- ❌ 未理解上下文就直接改代码。
- ❌ 一次提交大量不相关的修改。
- ❌ 复制粘贴重复逻辑（出现 2 次以上必须抽取）。
- ❌ 引入未经评估的第三方库。
- ❌ 删除或修改与当前任务无关的代码。
- ❌ 在不知道现有命名规范的情况下随意命名。

---

## 十一、上线前检查清单

- [ ] 所有页面在主流浏览器测试通过
- [ ] 移动端、平板、桌面端均显示正常
- [ ] 所有图片可正常加载且有 `alt`
- [ ] 所有链接、按钮功能正常
- [ ] 表单验证完整
- [ ] 控制台无报错、无警告
- [ ] 无遗留的调试代码
- [ ] HTML、CSS 通过 W3C 校验
- [ ] Lighthouse 各项评分达标
- [ ] SEO 元信息完整（title、description、og 标签等）
- [ ] favicon 已配置
- [ ] 404 页面已配置

---

## 十二、文档维护

- 本规范随项目演进持续更新。
- 任何规范的新增、修改需经团队讨论确认。
- 与本文档冲突的旧代码，遇到时顺手改正（在不偏离当前任务的前提下）。

---

**最后修改的具体日期时间**：2026-05-25 00:17:08（Asia/Shanghai）

---
