let noteS=`<div class="exp-block exp-block--note"><div class="exp-block-hd">${icNote}<span class="exp-block-tit">备注</span></div><textarea class="note-textarea" id="note_${t.id}" placeholder="添加备注…" onblur="saveNote(${t.id})">${esc(t.note||'')}</textarea></div>`;let tagS=`<div class="exp-meta-row"><div class="exp-meta-lbl">${icTag}<span>标签</span></div><div class="exp-meta-val"><div class="exp-tag-row">${customTags.map(tg=>{const on=(t.tags||[]).includes(tg.id);return`<span class="exp-tag-pill${on?' etp-on':''}" style="${on?'background:color-mix(in srgb,'+tg.color+' 22%,var(--acc-bg));color:var(--text);border-color:color-mix(in srgb,'+tg.color+' 55%,var(--acc-bd))':'border-color:var(--inp-bd)'}" onclick="toggleTaskTag(${t.id},'${tg.id}')">${tg.name}</span>`}).join('')}</div><button type="button" class="exp-tag-more" onclick="event.stopPropagation();navigate('/settings')" title="管理标签">+</button></div></div>`;let durS=`<div class="exp-meta-row"><div class="exp-meta-lbl">${icClock}<span>耗时</span></div><div class="exp-meta-val exp-meta-val--dur"><input type="number" id="dur_${t.id}" value="${t.duration||''}" min="0" max="480" placeholder="0" onchange="saveDuration(${t.id})"><span class="exp-unit">分钟</span></div></div>`;let colorS=`<div class="exp-meta-row"><div class="exp-meta-lbl">${icPalette}<span>颜色</span></div><div class="exp-meta-val"><div class="color-pick">${COLORS.map(c=>`<div class="color-opt${t.color===c?' co-sel':''}" style="background:${c||'var(--hov)'}" onclick="setTaskColor(${t.id},'${c}')">${c?'':'✕'}</div>`).join('')}</div></div></div>`;let prioS=`<div class="exp-meta-row"><div class="exp-meta-lbl">${icPrio}<span>优先级</span></div><div class="exp-meta-val"><button type="button" class="exp-prio-sel prio-high${(t.priority||'medium')==='high'?' on':''}" onclick="event.stopPropagation();togglePrioHighForTask(${t.id})">高</button></div></div>`;