# 👨‍💻 专属代码规范指南 (TypeScript/JavaScript)

## 1. 变量与命名规范
- **强制不可变性**：默认使用 `const`，只有在明确需要重新赋值的场景下才使用 `let`，严禁使用 `var`。
- **命名法**：
  - 变量、函数、方法名使用 `camelCase`（小驼峰）。
  - 类名、接口名（Interface）、类型别名（Type）使用 `PascalCase`（大驼峰）。
  - 常量（全局或模块级别）使用 `UPPER_SNAKE_CASE`（全大写下划线）。
- **语义化**：变量名必须能自解释，布尔值请使用 `is`、`has`、`should` 等前缀（如 `isVisible`）。

## 2. 函数与模块设计
- **单一职责原则**：一个函数只做一件事情。
- **长度限制**：单个函数体（不包含注释）尽量不超过 50 行，超过请考虑提取子函数。
- **函数式编程优先**：多使用 `map`、`filter`、`reduce` 等纯函数数组方法，减少使用 `for` 循环修改外部状态。
- **参数限制**：函数参数尽量不超过 3 个。如果超过，请使用对象（Object）进行解构传参。

## 3. 错误处理与健壮性
- **防御性编程**：处理对象属性或数组前，必须考虑 `null` 或 `undefined` 的情况（多用可选链 `?.` 和空值合并运算符 `??`）。
- **异常捕获**：异步操作（Promise/async-await）必须有 `try...catch` 或 `.catch()` 处理，并且要在 catch 块中进行合理的错误日志打印或抛出。

## 4. 类型规范 (TypeScript)
- **禁止 Any**：严禁使用 `any` 类型，如果类型暂时无法确定，请使用 `unknown` 并配合类型断言（Type Guard）。
- **接口优先**：定义对象结构时，优先使用 `interface`，需要交叉或联合类型时使用 `type`。